# Button + LED

An LED on **PB5** lights while a button on **PD2** is held (active-low, internal pull-up).

## On the breadboard

- Add an **LED** on **PB5**.
- Add a **Button** on **PD2**.

## Run

Run, then hold the button (⏺ hold) — the LED follows it.
