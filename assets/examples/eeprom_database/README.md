# EEPROM Configuration Database Example

This example shows how to perform byte-level read and write operations to the non-volatile EEPROM memory of the ATmega32.

## How to run
1. Start the simulation.
2. Open the **Console** tab on the Serial Monitor area.
3. Observe the logs indicating writing and successfully reading back configuration values.
