# 7-Segment Counter

Counts 0–9 on a common-cathode 7-segment digit. Segments a..g map to **PD0..PD6** (bit set = segment lit).

## On the breadboard

- Add a **7-segment** component; wire a..g to **PD0..PD6**.

## Run

Set Clock to 16 MHz, press Run — the digit counts up.
